package Tools.Image;

import javax.swing.ImageIcon;

// Interface que permite que elementos possuam imagens,
// todos os elementos implementam esta interface
public interface Drawable {
    public ImageIcon getImage();
}
